{% extends "page/checkout/inc/payment_other.html.twig" %}

{% block checkout_payment_longdesc %}
    {% if attribute(paymentmethod, 'isMolliePaymentMethod') is defined and paymentmethod.isMolliePaymentMethod() is same as(true) %}
        {% set paymentModel = paymentmethod.getMolliePaymentModel() %}
        {% if paymentModel and paymentModel.getCustomFrontendTemplate is not same as(false) %}
            {% include paymentModel.getCustomFrontendTemplate() %}
        {% endif %}
    {% endif %}
    {{ parent() }}
{% endblock %}